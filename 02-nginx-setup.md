# ラズベリーパイ ウェブサーバーラボ

## 目的

Raspberry Piを使って、自宅LAN内でWebサーバーを構築する学習記録です。
SSH接続、Linux操作、Webサーバーの導入、ネットワークの切り分けを学ぶことを目的としています。

## 環境

- Raspberry Pi
- Raspberry Pi OS
- TP-Linkルーター
- MacからSSH接続
- 自宅LAN内で検証

※セキュリティのため、IPアドレスや認証情報は記載しません。

## 今日やったこと

- 自機MacからRaspberry PiへのSSH公開鍵認証を完了する。
- .sshファイル内の整理整頓もかねてconfigファイルでconf.dファイルを参照させ、各接続先ごとの鍵をフォルダに整頓しました。
- ssh-agentを利用し、パスフレーズをスキップしてログイン可能にしました。
- nginxをインストールし、Macからnginxのデフォルトページを確認しました。
- シェルスクリプトを用いたnginxのインストール、自動起動設定、起動を行うファイルを作成しました。
- nginxをアンインストールし、上記のシェルスクリプトを実行し、正しい動作が行われているか確認しました。
- 以上を１時間で完了しました

## つまずいたこと

シェルスクリプトを実行する際、aptコマンド内でパスワード入力が求められ、止まってしまうことに悩みました。


## 確認したこと

シェルスクリプト内でsudoをaptコマンドにつけたり外したりして、動作を確認しました。

## 解決方法

起動コマンドを以下に変更しました。
"sh auto_nginx.sh "　→  "sudo auto_nginx.sh"
最初に１回入力すれば、求められることはありません。

## 学んだこと

- ssh-copy-idコマンドについて
- ssh-keygenについて
- シェルスクリプトの作成、実行方法
